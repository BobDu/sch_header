from setuptools import setup, find_packages


setup(
    name='sch_header',
    version='1.0.0',
    description='HTTPS Connect header.',
    packages=find_packages(),
    install_requires=['requests'],
)
